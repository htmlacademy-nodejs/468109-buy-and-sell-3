'use strict';

module.exports.offerKeys = [`category`, `description`, `picture`, `title`, `type`, `sum`];
module.exports.commentKeys = [`text`];

module.exports.entityNames = {
  OFFER: `offer`,
  COMMENT: `comment`
};
